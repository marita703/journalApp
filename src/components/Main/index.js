import { Form } from "../Form";
import { Box } from "../Box";
import { Entry } from "../Entry";

export function Main() {
  return (
    <main>
      <Form />
      <Box />
      <Entry />
    </main>
  );
}
